[project]
name = "src"
version = "0.0.1"
description = "An MLOps project for productionizing models"
authors = [{name = "Priyansh Singh", email = "priyanshsingh855@gmail.com"}]
dynamic = ["dependencies"]  # <--- This is required

[tool.setuptools]
packages = {find = {}}

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]} # <--- Should be a list
